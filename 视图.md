```Mermaid
graph TD
    User[前端用户] -->|请求 API| API[FastAPI 后端]
    
    subgraph "后端逻辑层"
    API -->|1. 先查缓存| Redis[(Redis 内存数据库)]
    Redis -->|2a. 命中 Hit| API
    
    API -->|2b. 未命中 Miss| DB[(MySQL 数据库)]
    DB -->|3. 返回数据| API
    API -->|4. 写入缓存 + 设过期时间| Redis
    end
    
    API -->|5. 返回结果| User
```